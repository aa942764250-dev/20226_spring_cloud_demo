# 本地 docker 部署：连服务器 Nacos(dev) + 远程 MySQL，关闭自动建表
spring:
  cloud:
    nacos:
      discovery:
        server-addr: 154.201.68.122:8848
        namespace: dev
        username: nacos
        password: nacos
        fail-fast: false
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://154.201.68.122:3306/springcloud_demo?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=Asia/Shanghai
    username: app_user
    password: App@Remote2026#User
  sql:
    init:
      mode: never
workbench:
  jwt:
    secret: ${WORKBENCH_JWT_SECRET:workbench-secret-key-must-be-at-least-256-bits-long-please-change}
