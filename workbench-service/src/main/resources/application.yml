server:
  port: 8083

spring:
  application:
    name: workbench-service
  cloud:
    nacos:
      discovery:
        server-addr: 154.201.68.122:8848
        namespace: dev
        username: nacos
        password: nacos
        fail-fast: false
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://154.201.68.122:3306/springcloud_demo?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=Asia/Shanghai
    username: app_user
    password: App@Remote2026#User

mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  global-config:
    db-config:
      id-type: auto
      logic-delete-field: deleted
      logic-delete-value: 1
      logic-not-delete-value: 0

workbench:
  jwt:
    secret: workbench-secret-key-must-be-at-least-256-bits-long-please-change
    expire-hours: 24
