# 开发环境配置 (dev namespace) —— 与 springcloud-service 共用远程 MySQL/Nacos
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://154.201.68.122:3306/springcloud_demo?useSSL=false&characterEncoding=utf8&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    username: app_user
    password: App@Remote2026#User
  cloud:
    nacos:
      discovery:
        server-addr: 154.201.68.122:8848
        namespace: dev
        username: nacos
        password: nacos
        fail-fast: false
  sql:
    init:
      mode: always
      encoding: UTF-8
      schema-locations: classpath:db/init_ai_teacher_tables.sql
